"""
SQL Optimization Challenge - Solution Stub

Implement two functions:
1. create_indexes(conn) - Create indexes to speed up the query
2. optimized_query() - Return an optimized SQL query string that produces
   the same results as slow_query.sql
"""

import sqlite3


def create_indexes(conn):
    """
    Create indexes on the database to speed up the query.
    conn is an open sqlite3.Connection.

    TODO: Add CREATE INDEX statements here.
    """
    pass


def optimized_query():
    """
    Return an optimized SQL query string that produces the exact same
    results as slow_query.sql (same columns, same order, same values).

    Returns:
        str: The optimized SQL query.

    TODO: Write your optimized query here.
    """
    return ""
